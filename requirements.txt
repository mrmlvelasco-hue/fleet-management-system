Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0
Flask-Login>=0.6
Flask-WTF>=1.2
WTForms>=3.1
email-validator>=2.0
marshmallow>=3.20
SQLAlchemy>=2.0
PyMySQL>=1.1
celery>=5.3
redis>=5.0
argon2-cffi>=23.1
python-dotenv>=1.0
pytest>=8.0
python-dateutil>=2.9
openpyxl
PyJWT>=2.7
gunicorn>=21.2

# Text extraction from scanned vehicle documents (Certificate of
# Registration). pytesseract is a thin wrapper around the tesseract
# BINARY, which the Dockerfile installs; without it the feature reports
# itself unavailable rather than failing. Pillow is already required
# transitively but pinned here since extraction depends on it directly.
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.0
